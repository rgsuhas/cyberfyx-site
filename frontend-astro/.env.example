# Public URL used by Astro at build/runtime when calling FastAPI directly.
# Leave empty in Docker/Nginx deployments that proxy /api to backend.
PUBLIC_API_BASE=http://127.0.0.1:8000

# Dev-server proxy target used by astro.config.mjs for /api requests.
API_PROXY_TARGET=http://127.0.0.1:8000
